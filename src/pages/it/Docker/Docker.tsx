import React from 'react';

const Docker: React.FC = () => {
    return (
        <div>
            <h1>Docker</h1>
            <p>Contenuti relativi a Docker.</p>
        </div>
    );
};

export default Docker;
