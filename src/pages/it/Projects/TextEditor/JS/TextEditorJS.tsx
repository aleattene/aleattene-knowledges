import React from "react";


const TextEditorJS: React.FC = () => {
    return (
        <div>
            <h1>Text Editor</h1>
            <p>Step:
                - Implementare un editor di testo con le seguenti funzionalità:
                    - inserimento di testo (finestra 1 di sinistra)
                    - selezione del tipo di formattazione (grassetto, corsivo, sottolineato) (navbar)
                    - visualizzazione live del testo formattato (finestra 2 di destra)
            </p>
        </div>
    );
};

export default TextEditorJS;

